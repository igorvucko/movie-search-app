/*export const ReviewItem = ({ id, author, content }: { id: string; author: string; content: string }) => (
    <li key={id}>
      <h4>{author}</h4>
      <p>{content.substring(0, 200)}...</p>
    </li>
  );

  */